spring:
  output:
    ansi:
      enabled: ALWAYS
  h2:
    console:
      enabled: true

  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:mem:simple
      username: sa
      password:

mybatis:
  mapper-locations:
    - mappers/**/*.xml

